# TavernLab 开发日志 — 2026-09-23（Day 2：数据主权 + 去 priority + 薄客户端）

## 一句话
角色 = 自包含数据包；预算去 priority 改「预计算 tokens + 8k/16k/32k 自动档 + 最近轮优先」；
Web/App 统一真源 `current_char`，App 变瘦镜像 + SSE/ntfy 推送。

## 数据模型：一角色一包（不变式强化）
- 布局改为 `data/characters/<名>/{card.json,avatar.*,chat.jsonl,media/,archive/}`。
  `session ≡ 角色名`；切角色只换读取目标，**绝不删除/覆盖/自动归档**。
- `internal/store`：`ChatMessage` 增 `id`/`images`；`SaveMedia`、`LoadArchive`、
  `MigrateLegacyChats`（旧 `data/chats/*.jsonl` + `chats/archive/*` 幂等搬入，不删原件）。
- 归档改 `characters/<名>/archive/<ts>.jsonl`。
- 静态 `/chars/` 只放行图片扩展名，`chat.jsonl`/`card.json`/archive 不再可被静态访问（原实现会泄露聊天）。

## 角色卡系统
- `card.json = {name, description, created}`；`POST /api/characters`（建卡，名字清洗+查重）、
  `PUT /api/characters/<名>/card`、`DELETE`（默认保留聊天，`?chats=1` 才删）。
- 不再抄 ST 宏系统：描述即自然语言，assemble 前由 `renderBlocks` 注入 character 块。

## 代码级注入（网页/App 共用）
- `renderBlocks`：`{{isodate}}{{date}}{{time}}{{weekday}}{{datetime}}{{timestamp}}{{timezone}}{{user}}`
  一次请求取一次 now；`source.type=="character"` 按 session 读 card 填 `{{character_card}}`。
- 修复：此前 `{{character_card}}`/时间宏从未替换，模型一直收到字面量。

## 预算引擎：去 priority（`internal/engine`）
- 删 `Priority`/按 priority 压缩；保留 `order` + 每块 `budget.max`。
- `EstimateTokens` 改分脚本（CJK≈1 字，ASCII≈4 字符/token），Audit 记估算 vs 上游真实 usage。
- `ContextConfig`：`tiers=[8192,16384,32768]`、`response_reserve`、`recent_chat_min_turns`。
- 组装：先算 fixed（system/时间/角色/distilled/RAG）→ 选最小可容纳档 → **chat 从最新一轮往回塞**；
  图片变大只挤旧轮，不再挤掉最近对话；超过最大档标 `overflow` 并尽力裁剪 elastic 块。
- 未解析模板（如未接的 distilled/vectra 仍含 `{{...}}`）直接 drop，避免污染 prompt。
- 修 `headCut` 方向：RAG 保高分头部、chat 保最新尾部。
- 单测 `engine_test.go`：档位选择、最近轮保护、overflow、未解析 drop。

## 实时与推送
- `broadcast.go`：内存 hub，`store.AppendChat` 后 `publish`；`GET /api/events?session=` SSE
  （25s ping）。App 前台订阅实时进 UI。
- ntfy：`publishNtfy`（settings 里 `ntfy_url`/`ntfy_topic`），Web 端助手回复推送到手机后台。
- 空回复不再写 JSONL。

## Web UI
- Chat head 清理：头像改圆角方框；只留「头像 + 角色名 + 心情文字」，删 `Prompt IDE · 16k…` 与顶栏按钮。
- 角色页改 88px 圆角卡片网格（头像+名+描述），删刷新；新建/编辑/删除走真接口；描述/头像编辑。
- 设置页收纳 导出 md / 导出全部归档 / 归档新楼 / 全局头像大小(88) / 窗口档位 / ntfy。
- Blocks 去 priority，显示 tier/overflow；滑杆改最大 token。

## App（Flutter，瘦镜像）
- 修「白色四方格」：`ImageIcon` 会把无 alpha 的 logo 压成实心色块 → 改 `ClipRRect + Image.asset`。
- AppBar 显示当前角色名，隐藏 model；右上角改 **Sync**（拉 `/api/history` 重建），不再删本地。
- 侧栏 `[TavernLab, 选角色, 设定, Sync]`，删 Tips / No chats found / 多楼列表。
- 发送只发新消息+未发送图片；图片由后端落 `media/` 并回灌 history。
- 设置页删 Behavior（system message / disable markdown / 警告）。
- 聊天气泡满宽；欢迎页换 TavernLab 版（删上游 Ollama 截图）。
- 角色选择写后端 `current_char`，网页/App 同源。

## 迁移 / 兼容
- 旧 `default.json` 的 priority 被忽略；character 块若源类型不是 character 会被当普通静态块。
- 旧聊天自动搬入角色包；旧 `data/settings.json` 缺新键走默认（current_char=Leer乐儿, user_name=RoyChong）。

## 验证
- `go build ./...` + `go test ./...` 全绿；本机起服务手测 建卡/assemble 注入/history 含图片/`/chars/` 拦 404。
- `flutter analyze` 仅 info；`flutter build apk --release` 出包成功（57.3MB）。

## 遗留 / 下一步
- distilled / vectra 源尚未实现（现会被 drop）；Intent Router 接入选档（8k/16k/32k 由意图决定）。
- App 图片消息 uri 用网络 URL 显示；离线缓存、编辑/删除消息、多角色 App 端已可用但需真机回归。
- ntfy 需在 m64 起服务并填 settings；主题建议加鉴权。
