# TavernLab 开发日志 — 2026-09-24（Day 3：可观测性 + 体验修复 + 蒸馏记忆 + systemd）

## 一句话
加了「日志模组」止血静默失败；把等待、传图、Blocks 拖动、设置保存这些日常体验补齐；
移植 Distilled Memory；常驻从 tmux 换成 systemd，端口统一 8888。

## 排障（起因）
- 「优化后运行不了」≠ one-api 密钥被覆盖：settings.json 被 gitignore，key 实测 200。
- 真因：旧进程占端口（新代码没起来）+ App host 硬编码 Tailscale IP + 全链路 `catch(_){}` 静默吞错。
- web 之所以「无反应」是免费 API 延迟 ~3 分钟；后端其实已生成并落盘（audit + chat.jsonl）。

## 可观测性（internal/obs）
- 结构化日志 + 环形缓冲（1000）+ 级别阈值；`GET/PUT /api/logs`、`GET /api/logs/stream`(SSE)。
- HTTP 中间件：method/path/query/status/ms/bytes/remote + panic-recover。
- 聊天路径每次 `turn` 日志（kind/status/model/tier/tokens/reply_len/mcp_hits）。
- proxy：SSE 转发去重复空行；上游非 2xx 改发 SSE `data:{"error":...}`。
- 前端 Logs 页（左侧栏「设置」上方）：过滤/实时/自动滚动/复制；`send()` 错误红色气泡 + 重试。

## Web UI
- 聊天区 flex 撑满贴边；等待时头像旁「努力回复中…」动画；`setPending()`。
- 按钮 `:active`/loading/toast；`asyncAction()` 助手。
- 设置/Memory/ntfy/Blocks **改动即自动保存**（debounce + toast）。
- Blocks：抓握柄竖向拖动、插入线、按 `id` 重排、`overflow-x:hidden`。
- 传图：composer「＋」、多图预览、粘贴上传；`images` dataURL 走后端 `saveImages`→`media/`。

## Distilled Memory（internal/distill）
- `characters/<名>/distilled.md` + `distilled.meta.json`（last_index/runs/last_run）。
- 每 8 用户轮后台自动蒸馏，或 Memory 页「立即蒸馏」；同会话不并发。
- 提示词默认取 `prompts.md` 蓝本（`{{user}}/{{isodate}}/{{weekday}}/{{time}}/{{maxchars}}`），可编辑。
- 注入 `distilled` block；结果以 `role=distilled_memory` 写 `chat.jsonl`（导出/chatToTurns 跳过）。
- `GET /api/distilled`、`POST /api/distilled/run`；settings 增 distill_* 字段。

## App（Flutter）
- `fixedHost` → `http://192.168.100.78:8888`；设置页去掉进入即探测，改手动 Test。
- `syncFromServer` 错误 SnackBar；新增 `startEvents()` 订阅 `/api/events?session=`。
- 附件「＋」解除 `multimodal` 门控恒可用。

## 部署
- 端口统一 **8888**（flag/serve.sh/App）；`deploy/tavernlab.service`（systemd，Restart=always）。
- m64：停旧 tmux 服务与 SillyTavern(tmux `nv`, 占 127.0.0.1:8888) → `systemctl enable --now tavernlab`。
- 验证：`0.0.0.0:8888` 监听；145/78 两个 IP 的 `/api/health` OK；e2e `session=test` 聊天返回并落 audit/日志。

## 遗留 / 下一步
- App 需重新出包验证 SSE/传图；Intent Router 选档；distilled schema 校验与待审队列；
- Blocks token 可视化；Audit 抽屉；旧会话(test/main)清理。
