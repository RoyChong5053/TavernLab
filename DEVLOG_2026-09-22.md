# TavernLab 开发日志 — 2026-09-22（Day 1）

## 一句话
决定脱离 SillyTavern，自研瘦前端+后端 `leer-chat`：**Go thin core + 静态 vanilla-JS 前端**，
定位不是“重写 ST”，而是 **能聊天的 Prompt IDE**。仓库：https://github.com/RoyChong5053/TavernLab.git（本地 `/home/roychong/app/TavernLab`）

## 背景动机
- RAG 已接入 MCP，`rag_mcp_server`（Qdrant）检索速度比 ST 快几个量级；opencode 对接 rag_mcp_server 很惊艳
- ST 真正有用的只有 Prompt 组装（context shift 16k + 固定 RAG 槽）；插件本质 = 修改最终 Prompt 的某个位置
- ST 95% 是累赘：多用户、webpack、extension loader、WorldInfo 全量解析、jQuery 屎山

## 架构定案（Plan v2，合并 ChatGPT 评审）
```
[手机/PC浏览器：静态JS，llama.cpp风格玻璃UI，无build]
  ↓ HTTP/SSE
[Go thin core：Prompt Engine + Context Budget + Chat Store + Audit + Memory Router]
  ├─ Vectra local（兜底，无AVX，随data/走，P0只读不写）
  └─ MCP client → rag_mcp_server/Qdrant（高端，LOQ独占）
[One-API：model/embed/rerank fan-out，6 channel/device]
[st-tele ws:2333 复用不变；TG 未来降级为单向通知，主链走 App 直连]
```
- **Context Budget Engine**（不叫 16k sliding window）：`order`=放哪里 vs `priority`=超预算谁先被压
  - LOCKED(100): time_anchor、system（time 置顶才有 time-aware 玄学，中段会被忽略）
  - HIGH(90): character、最近4轮、distilled
  - ELASTIC(40): RAG、旧chat；公式：Budget = MaxTokens − ResponseReserve（默认 16384−4096）
- **Audit 即核心**：每次生成存 `data/audit/<id>.json`（各块tokens/Dropped/raw/reply/upstream usage），支持 Replay / Edit&Replay
- **Memory Engine**（P2）：统一 `MemorySource` 接口（Search/Insert/Delete）→ VectraSource / MCPSource / DistilledSource；
  distilled 8-turn = World Lore 2.0，World Lore 废弃；per-character collection 路由（人生追踪/世界观测试切 collection 即可）
- **迁移**：二进制按 arch 分发 + `data/{chats,vectra,audit,memory,presets,characters,config.yaml}` 用 `rclone copy --copy-links` 搬家；无 sqlite 原生模块、无 AVX 依赖
- **语言**：Go（和 one-api、rag_mcp_server webUI 同栈，单二进制、老机常驻省内存）；代价：JS 资产（midterm-memory、st-tele、vectors）需重写
- **Vectra 不重写 writer**（ChatGPT 警告已接受）：Go 只做只读 adapter 读 ST 现有 `index.json`，否则 metadata 不兼容会破坏 rclone 无损迁移；LanceDB 放弃（不比 Vectra 强多少）

## P0 完成（已 push master）
- `internal/engine/`：Budget Engine（order排序 + ELASTIC优先压缩 + 单块Max截断）+ DefaultBlocks（system/time_anchor/character/distilled/rag_vectra/rag_mcp/chat）
- `main.go`：`/api/health`、`/api/blocks`（GET/PUT，文件 `data/presets/default.json`）、`/api/assemble`（干跑）、
  `/v1/chat/completions`（组装→透传 one-api→存 audit，支持 SSE 流式中继+重组）、`/api/audit`（列表/详情/replay stub）
- `internal/{memory,store,proxy}` + `web/` vanilla 玻璃 UI；实测 18080 端口 health/blocks/assemble 全通

## Expression Router 实验（已 push，6/6 通过）
- 问题：Leer 数字人需要开心/伤心/讲话/严肃/生气/大笑头像切换（webp 动图优先，wan2.2/minimax loop 视频以后换资源不换逻辑）
- ST 官方 `extensions/expressions` 机制已扒：不是 embedding，是 `transformers pipeline("text-classification")`
  （Extras `classify_module.py` / ST 内置 `src/endpoints/classify.js`），另有 llm/webllm 档（prompt：`Classify the emotion... Choose only one of: {{labels}}`）
- **关键想法验证通过**：reranker（11437 `qwen3-reranker-0.6b-q8_0`，`POST /v1/rerank`）可做 zero-shot 情绪分类（cross-encoder 原理）
  - 英文 label 描述句远稳于中文（0.94 vs 0.0008 级别分离）；中文回信作 query 没问题
  - joy/sad/angry/laugh 分辨率极高（几十~500倍差距）；talk/serious 属语气非情绪，reranker 全打 flat ~0.001，必须走规则
- 落地：`internal/expression/` 三级路由 `规则(talk/serious)→本地reranker(11437免费)→one-api小LLM兜底（待做）`，
  阈值 `floor 0.0015 + top1/top2>1.5`，miss 则 fallback 默认头且静默；`POST /api/expression/classify`；单次约 75ms、零花费
- 表情包切换 UI 等系统稳定后再接（P2），先只记 Audit

## UI 迭代（已 push `6f2ec57`）
- 左导航 6 页（llama.cpp 式）：💬聊天（默认）👤角色 🧱Blocks 🔍Audit 🧠Memory ⚙️设置；手机汉堡抽屉
- 聊天页顶角色头（头像圆框+名+表情徽标位）；角色页 `PUT /api/characters/:name/avatar`（multipart，原样存 webp/png/jpg/gif，浏览器原生播动态 webp），静态 `/chars/`；文件落 `data/characters/<名>/`
- Memory 页 P2 stub（只存偏好 localStorage）；Settings 预算+reranker地址存 localStorage
- llama.cpp UI 调研结论（`PORTING_LLAMA_UI.md`）：新版是 SvelteKit 重型工程（`tools/ui/src`，5.2M），整套搬违背免编译初衷，只借 UX（玻璃/自适应/SSE收流），结构自己重写；11436/11437 是 embedding/reranker 专用 server，无聊天 UI

## 运行与部署
- `go run . --port 8080 --data ./data --upstream http://127.0.0.1:3000`（`ONEAPI_KEY` 走环境变量）；浏览器开 `http://127.0.0.1:8080`
- 终局：家里旧电脑常驻（m64 还是 LOQ 待定），Tailscale 组网，手机 PWA/APK 直连；TG 主链砍掉
- APK 路线：PWA（manifest+SW）→ TWA（Bubblewrap 上架）→ 真有推送需求才 Capacitor 瘦客户端；全本地（手机跑 Go 后端）不做

## 下一步（按序）
1. P1：JSONL 聊天持久化（现只有 assemble/透传，chat 存档未接）、模型选择器、稳定跑几天（Tailscale+常驻）
2. P2：Memory（Distilled 8-turn 移植 + Vectra 读 + MCP）+ 表情 webp 切换 + Threshold 用 Leer 真实回信调优
3. P3：拖拽 Blocks、Token 可视化、Replay/Edit&Replay、PWA manifest、移动端打磨
4. 待定：家里固定跑哪台（m64/LOQ）、TG 是否保留离线通知、总预算按模型自适应（auto+response_reserve）

## 基础功能打通（已验证，未推）
- ① 设置页可写：`PUT/GET /api/settings` 落 `data/settings.json`（0600，已 gitignore，key 不进仓库）；
  优先级 启动参数 > `ONEAPI_KEY` > 设置文件；GET 只回 mask（`••••AcC1`）。前端 upstream/key(password)/rerank 可存，
  模型框变下拉（`GET /api/models` 后端代理 one-api，key 不过浏览器）。实测 key 有效，`auto-gemini` 在列
- ② Memory 接 `192.168.10.2:8199`（rag-mcp-server，Streamable HTTP，无鉴权）：`internal/mcp` 调
  `tools/call search_memory`，query=本轮用户原话，collection 空=服务端默认 global_memory；命中拆
  `--- Result N (score) ---` 进 `memory.Hit`，注入 `source.type=mcp` 的 block（`{{rag}}` 替换），20s 超时 fail-open；
  Memory 页服务端化（地址/collection/top-k/threshold/总开关+测试检索按钮）；Audit 新增 `memory{query,collection,hits,top,error}`
- ③ Blocks 拖拽（HTML5 DnD，落位重写 order）+ Preview 弹窗（当前编辑器状态试算：MCP 摘要+每块 tokens+全文
  `prompt_text`，头顶标明模拟非账单；Audit 保持真实 raw 不动）
- ④ 头像大小：`PUT /api/characters/:name/meta {avatar_px 24..480}` 存 `meta.json`（rclone 走），角色页滑杆；
  前端另有 localStorage 本机缩放（设置页，只影响本设备）；`--avatar-size` 思路用行内 style 落实
- 顺手修：`send()` 一直没传 `chat` turn（用户话根本到不了模型），`POST /v1/chat/completions` 补 `Chat` 字段；
  JSONL 落盘（`data/chats/<session>.jsonl`，user+assistant 双记，流式记重建文本）； Preview 走同一 `resolveMCP` 路径
- 全链实测（`早安，昨天睡得好吗？`→auto-gemini）：MCP 5 hits（top 0.998 睡眠对话）注入 rag_mcp 650tok，
  上游 `prompt 1957 / completion 897`，Audit+JSONL 双落盘 ✓
