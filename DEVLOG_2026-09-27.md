# TavernLab 开发日志 — 2026-09-27（Day 4：Android App 三个 bug + 发送可靠性 + Markdown 渲染）

## 一句话
修了三个用户报的 bug（缩略图串图、弱网静默失败、代码块看不到+表格难看），
把「发送」从一次性尝试改成带退避的持久队列，并给 Markdown 渲染补上真正能用的
代码框与表格。后端加了 `client_msg_id` 幂等，让自动重发不会双发。

## 排障（根因，不是症状）

### 1. 缩略图变成上一张图
`listBottomWidget` 给预览传的 `cacheKey` 是**常量字符串 `"pending0"`**，而
`buildImageWidget` 先查缓存、查到就直接 return，从不比对 uri。第二次选图命中第一次的
bytes，`gaplessPlayback: true` 再把旧帧冻住 → 显示上一张。发送是对的，因为
`onSendPressed` 读的是 `pendingImages`（真实 data URL），不是缓存。
`pruneImgCache` 还无条件 `keep.add("pending0")`，永不过期。

修：cacheKey 一律由内容派生（`imageCacheKey(uri)`）；气泡保留 `gapless`
（同一 uri 不闪），预览槽传 `gapless: false`（否则新图压在旧图上）。

### 2. 弱网静默失败（文字和图片都中招）
`onSendPressed` 末尾的 `catch (e)` 丢弃了 `e`，且漏了状态复位：

| 位置 | 问题 |
|---|---|
| `setAwaitingReply(true)` 之后 | catch 与 6 分钟 watchdog **都不复位** → 顶栏永远「努力回复中」 |
| catch 吞掉 `e` | `OllamaClientException(401/400)` 与 WiFi 抖动无法区分 |
| preflight `GET /api/health` | 不带 auth header 也不看 status，而 `/api/health` 在 authGate 白名单 → 凭证失效照样 200 放行 |
| `encodeXFileToDataURL` | 塞**完整原图**。4MB JPEG → 5.4MB base64 → 单个 JSON POST，不可续传 |

文字消息走同一条 shim 路径，抛出方式一样，所以同样静默。

修：
- **preflight 整个删掉**。flutter_chat_ui 的 `Input._handleSendPressed` 在回调返回后
  **同步**清空输入框，所以在这里拒绝会吃掉用户的草稿。改由队列兜底：轮次留在屏幕上、
  退避重试、并从真实响应里给出原因。
- `classifySendError` 把失败分成 network / auth / rejected / server，
  401 明确说「凭证失效，请检查 host headers」，400 直接透出后端的中文原因
  （`图片保存失败：…`），5xx/网络可重试。
- `awaitingReply` 在失败与终态时都复位，不再有永久 spinner。

### 3. 代码块超出范围看不到 / 表格难看
不是缺 scrollView，是**约束传递**：

`MarkdownStyleSheet.fitContent` 默认 true（`flutter_markdown-0.7.7+1/lib/src/widget.dart:513`
）→ `MarkdownBody` 内部 `Column` 用 `CrossAxisAlignment.start` → 每个 block 拿到
**松散**横向约束 → `CodeBlockFrame` 自身无宽度，内层 `Column(stretch, min)` 收缩包裹到
子元素固有宽度 = **未换行的最长代码行宽** → `SingleChildScrollView(horizontal)` 刚好
等于内容宽度，**溢出量为 0，无可滚动** → flutter_markdown 自己的
`Container(clipBehavior: Clip.hardEdge)` 再把溢出裁掉。
行短就正常、行长就看不到 → 正好是「有时候」。

表格：`tableColumnWidth` 默认 `FlexColumnWidth()`，不满足 flutter_markdown 自己的
`is FixedColumnWidth || is IntrinsicColumnWidth` 判定（`builder.dart:517`）→
`_buildTable()` **不套横向 Scrollbar/SCSV** → 宽表挤压换行。

修：给 `MarkdownBody` 传 **`fitContent: false`**（注意是 widget 的参数，不是 styleSheet 的）。
代码框与表格再各套一层 `SizedBox(width: double.infinity)` 兜底。

## 附带修掉的 bug：纯图发送按发送键没反应
`flutter_chat_ui` 的 `Input._handleSendPressed` 开头就 `if (trimmedText != '') return;`，
所以「只选图、不打字」时 `onSendPressed` **根本不会被调用**——而发送键因为
`sendButtonVisibilityMode: always` 是显示的，按下去毫无反应。后端一直支持纯图轮次
（`826b641`），是客户端够不到。
修：自己持有 `TextEditingController`，选图后若输入框为空就塞一个 `U+200B`
（Dart 的 `trim()` 不把它当空白，渲染上不可见），出队时剥掉。
同时这条 controller 也让「被拒绝的草稿」能被放回输入框。

## 发送可靠性：待发队列
新文件 `lib/outbox.dart`。一次尝试失败就消失的模型，换成有状态的：

```
queued ──flush──▶ sending ──ack──▶ (丢弃)
                     └──fail──▶ queued(退避) ──5min──▶ failed(红气泡+重试)
```

- 退避 2s → 5s → 15s → 30s → 60s，之后每 5 分钟一次
- 累计 5 分钟仍失败 → 终态 `failed` → 气泡下方红字 + 「重试」按钮 + snackbar
- 落盘：图片存 `getApplicationSupportDirectory()/tavernlab_outbox/<id>.<ext>`，
  SharedPreferences 只存元数据（几百 KB 的 base64 塞 prefs 会卡主线程）
- 触发：发送时、5s poller tick、App 回到前台、Sync 确认到某轮时
- `syncFromServer` 不再整体替换 `messages`：队列里还没被服务端确认的轮次会被保留并置顶
- 顺序：一次只发一条，后面的不能超车，否则服务端 JSONL 的聊天顺序会乱

### 后端幂等（自动重发的前提）
没有幂等的话重发会双发。`AppendChat` 原本在**服务端**生成 id，客户端重试无法识别。

- `store.AppendChatID(session, id, ...)`：支持客户端 id 作为幂等键；
  `sanitizeMessageID` 过滤非法形状（非法就静默退回普通 append，不拒绝这一轮）
- `store.FindTurn`：一次尾部读同时回答「这一轮存了吗」+「后面有回复吗」
  （只读文件尾 256KB / 400 行，让一轮的成本与聊天长度无关）
- `/api/chat` 收 `client_msg_id`：
  - 已存且**已有回复** → 直接回放存档回复（`"deduped": true`），不再调上游
  - 已存但**没有回复**（上次生成中途死了）→ 复用已存 media 重新生成，不重复存图
  - 未存 → 正常 append

真实服务器 e2e 验证（临时端口 + 每次请求自增的假上游，能看出是否重复生成）：

| 场景 | 结果 |
|---|---|
| 首次发送 | `pong-1`，1 个 media 文件，1 user + 1 assistant 行 |
| 同 id 重试（有回复） | `pong-1` + `"deduped":true`，**media 仍 1 个** |
| 同 id 重试（生成中途死了） | `pong-2`（确实重新生成），**media 仍 1 个**（读回原图，没存第二份） |

## Markdown 渲染（新增 `lib/markdown_widgets.dart`）
- **代码框**：双轴可滚（外层纵向 `maxHeight` = 屏高 50%，内层横向）、
  横向可滚时右侧渐隐提示、>12 行折叠 + 「展开剩余 N 行」、复制键（流式中隐藏，
  避免流式期间每帧 `setState` 抖动）
- **语法高亮**：`flutter_highlight` + 直接用 `highlight` 包（不用它的 `HighlightView`，
  它自带 Container 会破坏我自己的滚动布局）。语言别名表覆盖 LLM 常用 fence
  （`js/ts/py/sh/yml/c++/golang/tex…`）。三条约束：流式期间不高亮、
  >20000 字符不高亮、缓存键用 `lang:len:hash` 而不是整个 code（否则一份 400 行代码
  会在 key 和 span 树里各存一遍）
- **表格**：自定义 `table` builder → 表头**固定在纵向滚动区之外**、表头与所有行
  共享一个横向 `ScrollController`（`ScrollController` 持有一组 position，天生同步）、
  斑马纹、`:---:` 对齐、列宽 64–220px 有界（否则一个长散文单元会吃光整屏）
- **流式容错**（`lib/markdown_fix.dart`）：Dart 没有 remend，手写了一个保守版——
  补未闭合的 ``` / ~~~ / `` ` `` / `**` / `*` / `~~` / `$`，
  **只追加字符不删原文**，且不碰 fence 体内的内容（模板字符串里的 `**` 不是强调）。
  只在 `p0.id == liveStreamMsgId`（还在流）时套用，落地后按原文解析。
- **图片全屏**：`disableImageGallery: true` 以前是点图什么也不发生，现在可点开 +
  双指缩放。

## Day 4 追加：LaTeX + 发送重迭（同日第二轮）

### 发送中两个新讯息重迭
真机反馈：提示「发送中」时同一轮出现两个气泡，发完其中一个才消失。

根因是 `sendTurn` 里 `chatAllowed = false` 原本放在**收到响应头之后**，而服务端在
`AppendChatID` 落库那一刻就 `events.publish`。中间那个窗口里 SSE 触发了一次
`syncFromServer`：合并逻辑把「还在队列里的乐观气泡」和「服务端刚落库的同一轮」
都留了下来——服务端行的 id 就是 `client_msg_id`，但 live 流式气泡用的是另一个 id，
confirm 步骤只清乐观气泡、不清 live 气泡 → 同一句话渲染两次。流结束后
`liveStreamMsgId` 变 null，下一次 sync 才把 live 那个清掉，正好对上「发送完才消失」。

修三处：
1. `chatAllowed = false` 提到发请求**之前**（关掉窗口）。为不让新引入的
   `chatAllowed` 卡死，整个 `sendTurn` 用一个 try/finally 包住——图片编码、
   建连、非 2xx 任一失败都必须释放。
2. `syncFromServer` 在 `liveStreamMsgId != null` 时直接 return（poller 已经在等，
   但 SSE 和手动 Sync 不会，等于双保险）。
3. live 气泡 id 改成由 turnId 派生（`live-<turnId>`）而不是随机 uuid，confirm 时
   一并能认出来；同时流式插入从 insert 改成 **upsert**——重试会复用同一个派生 id，
   原来的 insert 要么重复要么把气泡冻在上一次的文本上。

顺带修一个同源问题：`renderOutboxTurn` 先插图再插文字，得到 [文字, 图]；
而 `fetchServerMessages` 每行先 append 文字再 append 图、最后 reverse，得到 [图, 文字]。
方向相反 → 服务端副本替换乐观副本的瞬间，图片和说明文字会**上下跳位**。已按
fetchServerMessages 的顺序对齐。

### LaTeX
新增 `lib/markdown_math.dart`，用 `flutter_math_fork`（纯 Dart，无 WebView 无网络，
和代码高亮同一取向；对应技术选型表里「KaTeX (WebView) 或原生 LaTeX 库」的后者）。

踩到的三个坑：
- **markdown 7.x 的 `InlineSyntax` 已经不是 `(pattern, tag)` 两参数了**，它变成抽象类，
  只能传 pattern，子类自己 `onMatch` 里 `parser.addNode(md.Element.text(tag, src))`
  造带 tag 的元素。
- **多行 `$$...$$` 不能用 InlineSyntax**。markdown 包是**逐行**跑 inline parser 的，
  开头的 `$$` 和结尾的 `$$` 一旦不在同一行就永远匹配不上（第一版就是这么写的，
  `$$\n...\n$$` 静默渲染成字面量）。必须自己写 `BlockSyntax`。
- **flutter_markdown 的 block tag 是写死的**（p/h1..h6/li/blockquote/pre/ol/ul/hr/
  table/thead/tbody/tr/section），遇到未知 block tag 会在
  `styleSheet.styles[tag]` 上做 `!` 解引用直接抛
  `Null check operator used on a null value`。所以 BlockSyntax 吐的是
  `Element('p', [Element.text(kMathBlockTag, src)])`——外层 `p` 让它走 inline 路径，
  和单行 `$$x$$` 原本能工作的原理一样。

另外 `$$` 未闭合时必须**在空行处收手**（和 BlockSyntax 的边界一致），否则正文里一个
手滑的 `$$` 会把后面整段回复都吞成 TeX。

降级（对应 `throwOnError: false`）：TeX 解析失败不抛异常、不留白，
`onErrorFallback` 回退成等宽字体的原始源码——流式到一半的公式很常见，
让人看见「本来想写什么」比看见报错有用。

同时把 `repairStreaming` 改成**认公式**：进入 `$$ ... $$` 后正文按字面量处理
（否则 `a * b` 里的孤立 `*` 会被当成没闭合的强调补一个 `*`，把公式改坏）；
只补 `$` 的条件也收窄成「这一行确实含 TeX 构造（`\cmd`、`^`、`_`）」，
否则流式过程中 `it costs $5` 会被闪成公式。真正的公式里有 `\frac`，
所以该补的时候还是补；纯数字 `$5` 不动。

## 测试
- `android_app/test/repair_and_outbox_test.dart`（36 个）：流式容错的正反例
  （含公式不被改坏、价格不被闪成公式）、语言别名、错误分类、队列退避/终态/JSON 往返
- `android_app/test/markdown_math_test.dart`（16 个）：**走完整 MarkdownBody 的
  端到端**断言，只测语法没用——还要确认 builder 真被调到。覆盖 `$x$` /
  `$$\frac{a}{b}$$` / 多行 `$$` / 两者混排 / 列表里 / 与粗体代码并存 /
  货币不被误判 / `\$` 转义 / 未闭合不吞整段 / TeX 报错降级成源码 /
  **代码块里的 `$$` 仍是代码**
- `android_app/test/widget_test.dart`（10 个）：**替换掉原来的 Flutter counter
  样板**（它在 `MainApp` 里找文本 '0'，从来就没通过过）。现在测的是两个真正的
  回归点：长代码行的 frame 宽度不超容器但 RichText 宽度超容器（证明横向确有溢出可滚）、
  表头不在纵向滚动区 descendant 里
- `main_test.go` +3：`AppendChatID` 去重、`FindTurn` 三态、media 读回
- `flutter analyze` 0 error（剩 13 条既有 info）
- APK 66.6MB（+4.5MB highlight 语法包，+3.8MB flutter_math_fork）

## 部署
- 后端：本机 commit+push → m64 `git pull --ff-only && go build -o tavernlab . && sudo systemctl restart tavernlab`
- App：`scp app-release.apk gt20:storage/downloads/TavernLab-2026-09-27.apk`

## 遗留 / 已知风险
- **必须在真机验**：连续选 3 张不同图（缩略图不能串）；飞行模式发文字/发图后恢复网络；
  300 行代码块；8 列宽表；半截 ``` 的流式；纯图不带文字直接发送
- **HEIC 风险**：`image_picker_android` 的 `ImageResizer` 用 `BitmapFactory` 解码，
  旧 Android 解不了 HEIC 时会**原样返回原文件**，压缩被绕过（只是变大，不会错）。
  gt20 默认拍 JPEG，大概率无影响，但值得在真机确认一次
- 后端仍无 body size 限制：客户端 1.5MB 闸门是唯一防线，Web 端上传还没跟上
- 表格是「固定表头 + 横向滚动」，不是二维滚动；单元格里的内联 markdown 目前按纯文本
  渲染（粗体/代码会丢），要保留得给 cell 嵌一层 MarkdownBody
- 队列上限 30 条，超了丢最老的（`kOutboxMax`）
- Mermaid 这轮没做
- 含公式的段落会被 flutter_markdown 拆成 `Wrap`（inline widget 的固有代价），
  公式前后的文字各自成为一个 Wrap 子项，换行位置可能和纯文字段落略有不同
- `$$a\nb$$ tail` 这种「闭标签后面还跟散文」的畸形写法，tail 会被并进公式
- `flutter_markdown` 已被官方标记 discontinued（替代品 `flutter_markdown_plus`），
  升级前要先确认 `builders` / `MarkdownElementBuilder` API 没变
